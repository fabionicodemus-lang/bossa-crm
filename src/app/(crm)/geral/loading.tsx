import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Contatos gerais" subtitle="Números do WhatsApp que ainda não são clientes nem corretores" variant="board" />;
}
