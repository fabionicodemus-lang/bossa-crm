import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Atendimento IA" subtitle="Nara e Plantão respondem, classificam e preparam a passagem para o humano" variant="list" />;
}
