import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Agenda" subtitle="Reuniões, apresentações, visitas, ligações e tarefas da equipe — inclusive as marcadas pela Nara e pelo Plantão." variant="list" />;
}
