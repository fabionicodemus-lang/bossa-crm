import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Clientes finais" subtitle="Pipeline comercial e histórico unificado" variant="board" />;
}
