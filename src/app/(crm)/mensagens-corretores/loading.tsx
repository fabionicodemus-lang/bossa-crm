import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Mensagens WhatsApp · Corretores" subtitle="Acompanhe as conversas do número dos corretores em uma caixa de entrada única" variant="chat" />;
}
