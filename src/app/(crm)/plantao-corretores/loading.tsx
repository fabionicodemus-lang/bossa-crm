import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Plantão IA · Corretores" subtitle="Defina exatamente quando a IA atende e quando as conversas ficam para o comercial" variant="list" />;
}
