import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Transmissões" subtitle="Modelos aprovados pela Meta, campanhas segmentadas e acompanhamento de entrega" variant="list" />;
}
