import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Propostas" subtitle="Simulação por datas, histórico por lead, PDF e planilha geral da operação comercial" variant="list" />;
}
