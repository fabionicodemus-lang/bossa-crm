import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Leads arquivados" subtitle="Contatos retirados das pipelines, com histórico preservado" variant="list" />;
}
