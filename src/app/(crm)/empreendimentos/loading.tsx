import { PageSkeleton } from '@/components/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Empreendimentos" subtitle="Cadastro, marcas, tipologias, materiais, estoque e condições comerciais" variant="list" />;
}
