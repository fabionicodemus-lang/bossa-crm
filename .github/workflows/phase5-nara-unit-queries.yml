name: Aplicar Fase 5 da Nara

on:
  push:
    branches:
      - feature/nara-prompt-v3-phase-5

permissions:
  contents: write

jobs:
  implement-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: sb_publishable_example
      SUPABASE_SECRET_KEY: sb_secret_example
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      NEXT_PUBLIC_ALLOW_PUBLIC_SIGNUP: "false"
      META_APP_ID: "123456789"
      META_APP_SECRET: test-meta-secret
      META_GRAPH_VERSION: v26.0
      WHATSAPP_WEBHOOK_VERIFY_TOKEN: test-webhook-token
      WHATSAPP_TOKEN_ENCRYPTION_KEY_BASE64: MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
      FEATURE_EMBEDDED_SIGNUP: "false"
      FEATURE_DIRECT_WHATSAPP_CONNECTION: "true"
      OPENAI_API_KEY: test-openai-key
      OPENAI_MODEL: gpt-5.6-terra
      OPENAI_MODEL_FALLBACK: gpt-5.6-luna
      CRON_SECRET: test-cron-secret
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/nara-prompt-v3-phase-5
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Implementar consultas comerciais da Nara
        shell: bash
        run: |
          python <<'PY'
          from pathlib import Path
          from textwrap import dedent
          import json

          module = dedent(r'''
          import type { SupabaseClient } from '@supabase/supabase-js';
          import type { Lead } from './types';

          type ChatMessage = { role: 'user' | 'assistant'; content: string };
          type JsonRecord = Record<string, unknown>;

          type DevelopmentRow = {
            id: string;
            name: string;
            slug: string;
            code: string | null;
            delivery_date: string | null;
          };

          type TypologyRow = {
            id: string;
            code: string;
            name: string;
            bedrooms: number | null;
            suites: number | null;
            private_area_m2: number | string | null;
          };

          type UnitRow = {
            id: string;
            development_id: string;
            typology_id: string | null;
            unit_code: string;
            floor: number | null;
            list_price: number | string;
            entry_amount: number | string;
            installment_count: number;
            installment_amount: number | string;
            payment_plan: JsonRecord | null;
            price_updated_at: string | null;
          };

          export type NaraUnitOffer = {
            empreendimento: string;
            unidade: string;
            andar: number | null;
            tipologia: string;
            valor: number;
            entrada: number;
            parcela_media: number | null;
            quantidade_parcelas: number;
            indice_correcao: string | null;
            validade_tabela: string | null;
            referencia_tabela: string | null;
            preco_atualizado_em: string | null;
          };

          export type NaraDevelopmentRange = {
            empreendimento: string;
            valor_minimo: number;
            valor_maximo: number;
            entrada_minima: number;
            qtd_disponiveis: number;
          };

          export type NaraApartmentFilters = {
            empreendimento: string;
            tipologia?: string | null;
            andar_min?: number | null;
            andar_max?: number | null;
            valor_max?: number | null;
            entrada_max?: number | null;
          };

          export type NaraCommercialCall = {
            name: 'faixa_empreendimento' | 'buscar_apartamentos' | 'consultar_apartamento';
            arguments: Record<string, unknown>;
            result: NaraDevelopmentRange | NaraUnitOffer[] | NaraUnitOffer | null;
          };

          export type NaraCommercialTurnContext = {
            consulted_at: string;
            source_table: 'development_units';
            calls: NaraCommercialCall[];
            source_text: string;
            error?: string;
          };

          const UNIT_SELECT = 'id,development_id,typology_id,unit_code,floor,list_price,entry_amount,installment_count,installment_amount,payment_plan,price_updated_at';
          const MONEY_IN_TEXT = /r\$\s*\d[\d.\s]*(?:,\d{1,2})?|\b\d+(?:[.,]\d+)?\s*(?:milhao|milhoes|mil)\b/giu;

          function normalizeText(value: unknown): string {
            return String(value ?? '')
              .normalize('NFD')
              .replace(/\p{Diacritic}/gu, '')
              .toLocaleLowerCase('pt-BR')
              .replace(/\s+/g, ' ')
              .trim();
          }

          function numberValue(value: unknown): number {
            const parsed = typeof value === 'number' ? value : Number(value ?? 0);
            return Number.isFinite(parsed) ? parsed : 0;
          }

          function optionalText(record: JsonRecord | null, keys: string[]): string | null {
            if (!record) return null;
            for (const key of keys) {
              const value = record[key];
              if (typeof value === 'string' && value.trim()) return value.trim();
              if (typeof value === 'number' && Number.isFinite(value)) return String(value);
            }
            return null;
          }

          function formatBrl(value: number): string {
            return new Intl.NumberFormat('pt-BR', {
              style: 'currency',
              currency: 'BRL',
              minimumFractionDigits: 2,
            }).format(value);
          }

          async function activeDevelopments(
            client: SupabaseClient,
            organizationId: string,
          ): Promise<DevelopmentRow[]> {
            const { data, error } = await client
              .from('developments')
              .select('id,name,slug,code,delivery_date')
              .eq('organization_id', organizationId)
              .eq('active', true)
              .order('name');
            if (error) throw error;
            return (data ?? []) as DevelopmentRow[];
          }

          async function resolveDevelopment(
            client: SupabaseClient,
            organizationId: string,
            requested: string,
          ): Promise<DevelopmentRow | null> {
            const rows = await activeDevelopments(client, organizationId);
            const target = normalizeText(requested);
            if (!target) return rows.length === 1 ? rows[0] : null;

            const exact = rows.find((row) => [row.name, row.slug, row.code]
              .some((value) => normalizeText(value) === target));
            if (exact) return exact;

            const alias = target.includes('flow') ? 'flow' : target.includes('alma') ? 'alma' : target;
            return rows.find((row) => {
              const candidates = [row.name, row.slug, row.code].map(normalizeText);
              return candidates.some((value) => value.includes(alias) || alias.includes(value));
            }) ?? null;
          }

          async function developmentTypologies(
            client: SupabaseClient,
            organizationId: string,
            developmentId: string,
          ): Promise<TypologyRow[]> {
            const { data, error } = await client
              .from('development_typologies')
              .select('id,code,name,bedrooms,suites,private_area_m2')
              .eq('organization_id', organizationId)
              .eq('development_id', developmentId)
              .eq('active', true);
            if (error) throw error;
            return (data ?? []) as TypologyRow[];
          }

          function typologyMatches(row: TypologyRow, requested: string): boolean {
            const target = normalizeText(requested);
            if (!target) return true;
            const text = normalizeText(`${row.code} ${row.name}`);
            if (text.includes(target) || target.includes(text)) return true;
            const suites = target.match(/\b(\d+)\s*suite/)?.[1];
            const bedrooms = target.match(/\b(\d+)\s*quarto/)?.[1];
            if (suites && row.suites === Number(suites)) return true;
            if (bedrooms && row.bedrooms === Number(bedrooms)) return true;
            return false;
          }

          function toOffer(
            development: DevelopmentRow,
            unit: UnitRow,
            typology: TypologyRow | undefined,
          ): NaraUnitOffer {
            const plan = unit.payment_plan && typeof unit.payment_plan === 'object'
              ? unit.payment_plan
              : null;
            return {
              empreendimento: development.name,
              unidade: unit.unit_code,
              andar: unit.floor,
              tipologia: typology?.name || typology?.code || 'Não informada',
              valor: numberValue(unit.list_price),
              entrada: numberValue(unit.entry_amount),
              parcela_media: unit.installment_count > 0 ? numberValue(unit.installment_amount) : null,
              quantidade_parcelas: Math.max(0, Number(unit.installment_count || 0)),
              indice_correcao: optionalText(plan, [
                'indice_correcao',
                'correction_index',
                'correction_label',
                'index_name',
              ]),
              validade_tabela: optionalText(plan, [
                'validade_tabela',
                'table_valid_until',
                'valid_until',
              ]),
              referencia_tabela: optionalText(plan, ['table_reference', 'referencia_tabela']),
              preco_atualizado_em: unit.price_updated_at,
            };
          }

          async function offersFromRows(
            client: SupabaseClient,
            organizationId: string,
            development: DevelopmentRow,
            rows: UnitRow[],
          ): Promise<NaraUnitOffer[]> {
            const typologies = await developmentTypologies(client, organizationId, development.id);
            const byId = new Map(typologies.map((row) => [row.id, row]));
            return rows.map((row) => toOffer(
              development,
              row,
              row.typology_id ? byId.get(row.typology_id) : undefined,
            ));
          }

          export async function faixaEmpreendimento(
            client: SupabaseClient,
            organizationId: string,
            empreendimento: string,
          ): Promise<NaraDevelopmentRange | null> {
            const development = await resolveDevelopment(client, organizationId, empreendimento);
            if (!development) return null;
            const { data, error } = await client
              .from('development_units')
              .select('list_price,entry_amount')
              .eq('organization_id', organizationId)
              .eq('development_id', development.id)
              .eq('status', 'disponivel')
              .gt('list_price', 0)
              .order('list_price', { ascending: true });
            if (error) throw error;
            const rows = (data ?? []) as Array<{ list_price: number | string; entry_amount: number | string }>;
            if (!rows.length) return null;
            const prices = rows.map((row) => numberValue(row.list_price)).filter((value) => value > 0);
            const entries = rows.map((row) => numberValue(row.entry_amount)).filter((value) => value > 0);
            if (!prices.length) return null;
            return {
              empreendimento: development.name,
              valor_minimo: Math.min(...prices),
              valor_maximo: Math.max(...prices),
              entrada_minima: entries.length ? Math.min(...entries) : 0,
              qtd_disponiveis: rows.length,
            };
          }

          export async function buscarApartamentos(
            client: SupabaseClient,
            organizationId: string,
            filters: NaraApartmentFilters,
          ): Promise<NaraUnitOffer[]> {
            const development = await resolveDevelopment(client, organizationId, filters.empreendimento);
            if (!development) return [];

            const typologies = await developmentTypologies(client, organizationId, development.id);
            const matchingTypologyIds = filters.tipologia
              ? typologies.filter((row) => typologyMatches(row, filters.tipologia || '')).map((row) => row.id)
              : [];
            if (filters.tipologia && matchingTypologyIds.length === 0) return [];

            let query = client
              .from('development_units')
              .select(UNIT_SELECT)
              .eq('organization_id', organizationId)
              .eq('development_id', development.id)
              .eq('status', 'disponivel')
              .gt('list_price', 0);
            if (matchingTypologyIds.length) query = query.in('typology_id', matchingTypologyIds);
            if (Number.isFinite(filters.andar_min)) query = query.gte('floor', Number(filters.andar_min));
            if (Number.isFinite(filters.andar_max)) query = query.lte('floor', Number(filters.andar_max));
            if (Number.isFinite(filters.valor_max)) query = query.lte('list_price', Number(filters.valor_max));
            if (Number.isFinite(filters.entrada_max)) query = query.lte('entry_amount', Number(filters.entrada_max));

            const { data, error } = await query
              .order('list_price', { ascending: true })
              .order('floor', { ascending: true, nullsFirst: false })
              .limit(3);
            if (error) throw error;
            return offersFromRows(client, organizationId, development, (data ?? []) as UnitRow[]);
          }

          export async function consultarApartamento(
            client: SupabaseClient,
            organizationId: string,
            empreendimento: string,
            unidade: string,
          ): Promise<NaraUnitOffer | null> {
            const development = await resolveDevelopment(client, organizationId, empreendimento);
            if (!development) return null;
            const { data, error } = await client
              .from('development_units')
              .select(UNIT_SELECT)
              .eq('organization_id', organizationId)
              .eq('development_id', development.id)
              .eq('unit_code', unidade.trim())
              .eq('status', 'disponivel')
              .gt('list_price', 0)
              .maybeSingle();
            if (error) throw error;
            if (!data) return null;
            const offers = await offersFromRows(client, organizationId, development, [data as UnitRow]);
            return offers[0] ?? null;
          }

          function formatRange(result: NaraDevelopmentRange | null): string {
            if (!result) return 'resultado vazio';
            return [
              `empreendimento=${result.empreendimento}`,
              `valor_minimo=${formatBrl(result.valor_minimo)}`,
              `valor_maximo=${formatBrl(result.valor_maximo)}`,
              `entrada_minima=${formatBrl(result.entrada_minima)}`,
              `qtd_disponiveis=${result.qtd_disponiveis}`,
            ].join('; ');
          }

          function formatOffer(result: NaraUnitOffer): string {
            return [
              `empreendimento=${result.empreendimento}`,
              `unidade=${result.unidade}`,
              `andar=${result.andar ?? 'não informado'}`,
              `tipologia=${result.tipologia}`,
              `valor=${formatBrl(result.valor)}`,
              `entrada=${formatBrl(result.entrada)}`,
              result.parcela_media === null ? 'parcela_media=não informada' : `parcela_media=${formatBrl(result.parcela_media)}`,
              `quantidade_parcelas=${result.quantidade_parcelas}`,
              `indice_correcao=${result.indice_correcao ?? 'não informado'}`,
              `validade_tabela=${result.validade_tabela ?? 'não informada'}`,
              `referencia_tabela=${result.referencia_tabela ?? 'não informada'}`,
              `preco_atualizado_em=${result.preco_atualizado_em ?? 'não informado'}`,
            ].join('; ');
          }

          function sourceText(calls: NaraCommercialCall[]): string {
            const lines = calls.flatMap((call) => {
              if (call.name === 'faixa_empreendimento') {
                return [`[faixa_empreendimento] ${formatRange(call.result as NaraDevelopmentRange | null)}`];
              }
              if (Array.isArray(call.result)) {
                const rows = call.result as NaraUnitOffer[];
                return rows.length
                  ? rows.map((row, index) => `[buscar_apartamentos ${index + 1}/${rows.length}] ${formatOffer(row)}`)
                  : ['[buscar_apartamentos] resultado vazio'];
              }
              return call.result
                ? [`[consultar_apartamento] ${formatOffer(call.result as NaraUnitOffer)}`]
                : ['[consultar_apartamento] resultado vazio'];
            });
            return lines.join('\n');
          }

          function lastUserMessage(history: ChatMessage[]): string {
            return [...history].reverse().find((item) => item.role === 'user')?.content ?? '';
          }

          function allUserText(history: ChatMessage[]): string {
            return history.filter((item) => item.role === 'user').map((item) => item.content).join(' ');
          }

          function inferredEnterprise(lead: Lead, history: ChatMessage[]): string {
            const latest = normalizeText(lastUserMessage(history));
            if (latest.includes('flow')) return 'flow';
            if (latest.includes('alma')) return 'alma';
            const leadEnterprise = normalizeText(lead.enterprise);
            if (leadEnterprise.includes('flow')) return 'flow';
            if (leadEnterprise.includes('alma')) return 'alma';
            const historyText = normalizeText(allUserText(history));
            if (historyText.includes('flow')) return 'flow';
            if (historyText.includes('alma')) return 'alma';
            const metadataText = normalizeText(JSON.stringify(lead.metadata ?? {}));
            if (metadataText.includes('flow')) return 'flow';
            if (metadataText.includes('alma')) return 'alma';
            return '';
          }

          function unitCodeFromMessage(message: string): string {
            const value = normalizeText(message);
            const explicit = value.match(/\b(?:unidade|apto|apartamento)\s*(?:n(?:umero)?\s*)?(\d{2,4})\b/)?.[1];
            if (explicit) return explicit;
            const withoutMoney = value.replace(MONEY_IN_TEXT, ' ');
            const candidates = withoutMoney.match(/\b\d{3,4}\b/g) ?? [];
            return candidates.find((item) => !/^20(?:2\d|3\d)$/.test(item)) ?? '';
          }

          function numberAfter(message: string, expression: RegExp): number | null {
            const match = normalizeText(message).match(expression)?.[1];
            if (!match) return null;
            const raw = match.replace(/\./g, '').replace(',', '.');
            const value = Number(raw);
            if (!Number.isFinite(value)) return null;
            const normalized = normalizeText(message);
            if (/milhao|milhoes/.test(normalized)) return value * 1_000_000;
            if (/\bmil\b/.test(normalized)) return value * 1_000;
            return value;
          }

          function filtersFromMessage(enterprise: string, message: string): NaraApartmentFilters {
            const normalized = normalizeText(message);
            const suites = normalized.match(/\b(\d+)\s*suites?\b/)?.[1];
            const bedrooms = normalized.match(/\b(\d+)\s*quartos?\b/)?.[1];
            const exactFloor = normalized.match(/\b(?:andar|no)\s*(\d{1,2})\b/)?.[1];
            const aboveFloor = normalized.match(/\b(?:acima|a partir)\s+(?:do |de )?(\d{1,2})(?:o|º)?\s*andar\b/)?.[1];
            const belowFloor = normalized.match(/\b(?:ate|abaixo)\s+(?:o |do )?(\d{1,2})(?:o|º)?\s*andar\b/)?.[1];
            return {
              empreendimento: enterprise,
              tipologia: suites ? `${suites} suítes` : bedrooms ? `${bedrooms} quartos` : normalized.includes('duplex') ? 'duplex' : null,
              andar_min: aboveFloor ? Number(aboveFloor) : exactFloor ? Number(exactFloor) : null,
              andar_max: belowFloor ? Number(belowFloor) : exactFloor ? Number(exactFloor) : null,
              valor_max: numberAfter(message, /(?:valor|preco|ate)\D{0,12}(\d[\d.,]*)/),
              entrada_max: numberAfter(message, /entrada\D{0,12}(\d[\d.,]*)/),
            };
          }

          function hasCommercialSignal(message: string): boolean {
            return /\b(precos?|valores?|quanto custa|faixa|a partir de|tabela|disponiv|unidade|apto|apartamento|entrada|parcela|andar|suites?|quartos?|duplex)\b/.test(normalizeText(message));
          }

          function asksForSpecificOptions(message: string): boolean {
            return /\b(disponiv|unidade|apto|apartamento|entrada|parcela|andar|suites?|quartos?|duplex|opcoes?)\b/.test(normalizeText(message));
          }

          export async function loadNaraCommercialTurnContext(
            client: SupabaseClient,
            organizationId: string,
            lead: Lead,
            history: ChatMessage[],
          ): Promise<NaraCommercialTurnContext | null> {
            if (lead.kind !== 'cliente') return null;
            const latest = lastUserMessage(history);
            if (!latest || !hasCommercialSignal(latest)) return null;
            const consultedAt = new Date().toISOString();
            const calls: NaraCommercialCall[] = [];

            try {
              const enterprise = inferredEnterprise(lead, history);
              const unitCode = unitCodeFromMessage(latest);

              if (unitCode) {
                if (!enterprise) {
                  return {
                    consulted_at: consultedAt,
                    source_table: 'development_units',
                    calls: [],
                    source_text: 'A mensagem menciona uma unidade, mas não identifica o empreendimento. Não informe preço ou disponibilidade dessa unidade; pergunte se é Flow ou Alma.',
                  };
                }
                const result = await consultarApartamento(client, organizationId, enterprise, unitCode);
                calls.push({
                  name: 'consultar_apartamento',
                  arguments: { empreendimento: enterprise, unidade: unitCode },
                  result,
                });
              } else if (enterprise && asksForSpecificOptions(latest)) {
                const filters = filtersFromMessage(enterprise, latest);
                const result = await buscarApartamentos(client, organizationId, filters);
                calls.push({ name: 'buscar_apartamentos', arguments: filters, result });
              } else if (enterprise) {
                const result = await faixaEmpreendimento(client, organizationId, enterprise);
                calls.push({
                  name: 'faixa_empreendimento',
                  arguments: { empreendimento: enterprise },
                  result,
                });
              } else {
                const developments = await activeDevelopments(client, organizationId);
                for (const development of developments) {
                  const result = await faixaEmpreendimento(client, organizationId, development.name);
                  calls.push({
                    name: 'faixa_empreendimento',
                    arguments: { empreendimento: development.name },
                    result,
                  });
                }
              }

              return {
                consulted_at: consultedAt,
                source_table: 'development_units',
                calls,
                source_text: sourceText(calls),
              };
            } catch (error) {
              const message = error instanceof Error ? error.message : String(error);
              console.error('[nara commercial query]', message);
              return {
                consulted_at: consultedAt,
                source_table: 'development_units',
                calls: [],
                source_text: 'A consulta comercial do sistema não respondeu neste turno. Não informe números lembrados ou estimados; diga que o comercial confirmará a condição vigente.',
                error: message,
              };
            }
          }
          ''').strip() + '\n'
          Path('src/lib/nara-unit-queries.ts').write_text(module, encoding='utf-8')

          test = dedent(r'''
          import assert from 'node:assert/strict';
          import { readFile } from 'node:fs/promises';
          import {
            buscarApartamentos,
            consultarApartamento,
            faixaEmpreendimento,
            loadNaraCommercialTurnContext,
          } from '../src/lib/nara-unit-queries.ts';

          class Query {
            constructor(rows) {
              this.rows = rows;
              this.filters = [];
              this.orders = [];
              this.maxRows = null;
            }
            select() { return this; }
            eq(column, value) { this.filters.push((row) => row[column] === value); return this; }
            gt(column, value) { this.filters.push((row) => Number(row[column]) > Number(value)); return this; }
            gte(column, value) { this.filters.push((row) => Number(row[column]) >= Number(value)); return this; }
            lte(column, value) { this.filters.push((row) => Number(row[column]) <= Number(value)); return this; }
            in(column, values) { this.filters.push((row) => values.includes(row[column])); return this; }
            order(column, options = {}) { this.orders.push({ column, ascending: options.ascending !== false }); return this; }
            limit(value) { this.maxRows = value; return this; }
            execute() {
              let data = this.rows.filter((row) => this.filters.every((filter) => filter(row)));
              for (const order of [...this.orders].reverse()) {
                data = [...data].sort((a, b) => {
                  const left = a[order.column];
                  const right = b[order.column];
                  const result = typeof left === 'number' && typeof right === 'number'
                    ? left - right
                    : String(left ?? '').localeCompare(String(right ?? ''));
                  return order.ascending ? result : -result;
                });
              }
              if (this.maxRows !== null) data = data.slice(0, this.maxRows);
              return Promise.resolve({ data, error: null });
            }
            then(resolve, reject) { return this.execute().then(resolve, reject); }
            maybeSingle() {
              return this.execute().then(({ data, error }) => ({ data: data[0] ?? null, error }));
            }
          }

          class FakeSupabase {
            constructor(tables) { this.tables = tables; }
            from(name) { return new Query(this.tables[name] ?? []); }
          }

          const developments = [
            { id: 'flow', organization_id: 'org', name: 'Flow Aptos', slug: 'flow-aptos', code: 'FLOW', delivery_date: '2027-11-01', active: true },
            { id: 'alma', organization_id: 'org', name: 'Alma Seahouses', slug: 'alma-seahouses', code: 'ALMA', delivery_date: '2030-07-01', active: true },
          ];
          const typologies = [
            { id: 'flow-2s', organization_id: 'org', development_id: 'flow', code: '02', name: '2 suítes', bedrooms: 2, suites: 2, private_area_m2: 62, active: true },
            { id: 'flow-3q', organization_id: 'org', development_id: 'flow', code: '03', name: '3 quartos', bedrooms: 3, suites: 1, private_area_m2: 73, active: true },
            { id: 'alma-3s', organization_id: 'org', development_id: 'alma', code: '01', name: '3 suítes', bedrooms: 3, suites: 3, private_area_m2: 120, active: true },
          ];
          const basePlan = { table_reference: '2026-08', indice_correcao: 'CUB/SC' };
          const units = [
            { id: 'f901', organization_id: 'org', development_id: 'flow', typology_id: 'flow-2s', unit_code: '901', floor: 9, status: 'disponivel', list_price: 900000, entry_amount: 180000, installment_count: 60, installment_amount: 4500, payment_plan: basePlan, price_updated_at: '2026-08-04T12:00:00Z' },
            { id: 'f1001', organization_id: 'org', development_id: 'flow', typology_id: 'flow-2s', unit_code: '1001', floor: 10, status: 'disponivel', list_price: 950000, entry_amount: 190000, installment_count: 60, installment_amount: 4750, payment_plan: basePlan, price_updated_at: '2026-08-04T12:00:00Z' },
            { id: 'f1101', organization_id: 'org', development_id: 'flow', typology_id: 'flow-3q', unit_code: '1101', floor: 11, status: 'disponivel', list_price: 1000000, entry_amount: 200000, installment_count: 60, installment_amount: 5000, payment_plan: basePlan, price_updated_at: '2026-08-04T12:00:00Z' },
            { id: 'f1201', organization_id: 'org', development_id: 'flow', typology_id: 'flow-3q', unit_code: '1201', floor: 12, status: 'disponivel', list_price: 1050000, entry_amount: 210000, installment_count: 60, installment_amount: 5250, payment_plan: basePlan, price_updated_at: '2026-08-04T12:00:00Z' },
            { id: 'f1301', organization_id: 'org', development_id: 'flow', typology_id: 'flow-3q', unit_code: '1301', floor: 13, status: 'reservado', list_price: 800000, entry_amount: 160000, installment_count: 60, installment_amount: 4000, payment_plan: basePlan, price_updated_at: '2026-08-04T12:00:00Z' },
            { id: 'a901', organization_id: 'org', development_id: 'alma', typology_id: 'alma-3s', unit_code: '901', floor: 9, status: 'disponivel', list_price: 1344231.08, entry_amount: 201634.66, installment_count: 80, installment_amount: 5376.92, payment_plan: { table_reference: '2026-08' }, price_updated_at: '2026-08-04T12:00:00Z' },
          ];
          const client = new FakeSupabase({ developments, development_typologies: typologies, development_units: units });

          const range = await faixaEmpreendimento(client, 'org', 'Flow');
          assert.deepEqual(range, {
            empreendimento: 'Flow Aptos',
            valor_minimo: 900000,
            valor_maximo: 1050000,
            entrada_minima: 180000,
            qtd_disponiveis: 4,
          });

          const found = await buscarApartamentos(client, 'org', { empreendimento: 'Flow', tipologia: '3 quartos' });
          assert.equal(found.length, 2);
          assert.deepEqual(found.map((row) => row.unidade), ['1101', '1201']);
          assert.ok(found.every((row) => row.valor >= 1000000));

          const capped = await buscarApartamentos(client, 'org', { empreendimento: 'Flow' });
          assert.equal(capped.length, 3);
          assert.deepEqual(capped.map((row) => row.unidade), ['901', '1001', '1101']);

          const available = await consultarApartamento(client, 'org', 'Flow', '901');
          assert.equal(available?.valor, 900000);
          assert.equal(available?.indice_correcao, 'CUB/SC');
          const unavailable = await consultarApartamento(client, 'org', 'Flow', '1301');
          assert.equal(unavailable, null);

          const lead = {
            id: 'lead', organization_id: 'org', kind: 'cliente', name: 'Teste', stage: 'ia',
            enterprise: 'Flow Aptos', metadata: {},
          };
          const context = await loadNaraCommercialTurnContext(client, 'org', lead, [
            { role: 'user', content: 'Quanto custa o Flow?' },
          ]);
          assert.equal(context?.calls[0]?.name, 'faixa_empreendimento');
          assert.match(context?.source_text ?? '', /R\$\s*900\.000,00/);
          assert.doesNotMatch(context?.source_text ?? '', /reservado/);

          const exactContext = await loadNaraCommercialTurnContext(client, 'org', lead, [
            { role: 'user', content: 'Quanto custa a 901?' },
          ]);
          assert.equal(exactContext?.calls[0]?.name, 'consultar_apartamento');
          assert.equal(exactContext?.calls[0]?.result?.unidade, '901');

          const aiSource = await readFile(new URL('../src/lib/ai.ts', import.meta.url), 'utf8');
          const webhookSource = await readFile(new URL('../src/lib/whatsapp/webhookProcessor.ts', import.meta.url), 'utf8');
          const simulatorSource = await readFile(new URL('../src/app/api/ai-training/route.ts', import.meta.url), 'utf8');
          assert.match(aiSource, /context\.commercial\?\.source_text/);
          assert.match(aiSource, /CONSULTAS COMERCIAIS DESTE TURNO/);
          assert.match(aiSource, /base de conhecimento, e o retorno das consultas comerciais/);
          assert.match(webhookSource, /loadNaraCommercialTurnContext/);
          assert.match(simulatorSource, /loadNaraCommercialTurnContext/);

          console.log('Consultas da Nara validadas: somente disponíveis, teto de 3 e valores fundamentados no turno.');
          ''').strip() + '\n'
          Path('scripts/check-nara-unit-queries.mjs').write_text(test, encoding='utf-8')

          ai_path = Path('src/lib/ai.ts')
          ai = ai_path.read_text(encoding='utf-8')
          old = "import { asksProtectedCommercialDetail, isGeneralPriceRangeReply } from './nara-price-levels';\nimport type { Lead } from './types';"
          new = "import { asksProtectedCommercialDetail, isGeneralPriceRangeReply } from './nara-price-levels';\nimport type { NaraCommercialTurnContext } from './nara-unit-queries';\nimport type { Lead } from './types';"
          if old not in ai: raise SystemExit('Importação de ai.ts não encontrada')
          ai = ai.replace(old, new, 1)

          old = "  files?: AiFileOption[];\n}"
          new = "  files?: AiFileOption[];\n  commercial?: NaraCommercialTurnContext | null;\n}"
          if old not in ai: raise SystemExit('Interface AiTrainingContext não encontrada')
          ai = ai.replace(old, new, 1)

          old = "- Um pedido isolado de preço, valor, tabela, menor apartamento, planta ou disponibilidade não confirma sozinho que o contato é comprador.\\n- Se a primeira mensagem pedir preço sem declarar finalidade de compra, moradia ou investimento, não informe valor nesta fase. Use a pergunta de triagem configurada e aguarde. Esta proteção será revista na fase específica de preço."
          new = "- Um pedido isolado de preço, valor, tabela, menor apartamento, planta ou disponibilidade não confirma sozinho que o contato é comprador.\\n- Quando houver retorno de faixa_empreendimento nas consultas comerciais deste turno, você pode informar somente a faixa geral mesmo sem intenção confirmada. Isso não libera qualificação, arquivos, tabela, unidade, disponibilidade ou condição específica.\\n- Unidade, andar, disponibilidade, entrada, parcela e condição específica só podem ser informados quando a intenção de compra estiver confirmada e houver retorno correspondente da consulta comercial no mesmo turno.\\n- Se a consulta comercial estiver vazia ou indisponível, não use valores lembrados: diga que o comercial confirmará a condição vigente."
          if old not in ai: raise SystemExit('Regras antigas de preço não encontradas')
          ai = ai.replace(old, new, 1)

          old = "function dynamicLeadContext(lead: Lead): string {\n  return `DADOS DINÂMICOS DESTA CONVERSA:\\nContato: ${lead.name}.\\nEtapa atual: ${lead.stage}.\\nDados atuais: ${JSON.stringify(lead.metadata || {})}.`;\n}"
          new = "function dynamicLeadContext(lead: Lead, context: AiTrainingContext): string {\n  const commercial = context.commercial?.source_text?.trim();\n  return `DADOS DINÂMICOS DESTA CONVERSA:\\nContato: ${lead.name}.\\nEtapa atual: ${lead.stage}.\\nDados atuais: ${JSON.stringify(lead.metadata || {})}.${commercial ? `\\n\\nCONSULTAS COMERCIAIS DESTE TURNO — FONTE ATUAL DO SISTEMA:\\n${commercial}\\n\\nUse somente esses retornos para preço e disponibilidade neste turno. Nunca mencione nomes internos de função ou banco. Resultado vazio significa que não há unidade disponível comprovada para informar, sem explicar o motivo.` : ''}`;\n}"
          if old not in ai: raise SystemExit('dynamicLeadContext não encontrado')
          ai = ai.replace(old, new, 1)
          ai = ai.replace("`${dynamicLeadContext(lead)}${summary ?", "`${dynamicLeadContext(lead, context)}${summary ?", 1)

          old = "    recordText(context.config?.knowledge),\n  ].join('\\n');"
          new = "    recordText(context.config?.knowledge),\n    context.commercial?.source_text ?? '',\n  ].join('\\n');"
          if old not in ai: raise SystemExit('Corpus da trava monetária não encontrado')
          ai = ai.replace(old, new, 1)

          old = "- Nunca use preços lembrados pelo modelo. Um valor só pode ser informado quando estiver explicitamente em uma fonte válida para o turno."
          new = "- Nunca use preços lembrados pelo modelo. Um valor só pode ser informado quando estiver explicitamente nas mensagens do contato, na base de conhecimento, ou no retorno das consultas comerciais do sistema no mesmo turno."
          if old not in ai: raise SystemExit('Regra de fonte monetária não encontrada')
          ai = ai.replace(old, new, 1)
          ai_path.write_text(ai, encoding='utf-8')

          webhook_path = Path('src/lib/whatsapp/webhookProcessor.ts')
          webhook = webhook_path.read_text(encoding='utf-8')
          old = "import { loadAiContext } from '@/lib/ai-context';\nimport { recordAiUsage } from '@/lib/ai-usage';"
          new = "import { loadAiContext } from '@/lib/ai-context';\nimport { recordAiUsage } from '@/lib/ai-usage';\nimport { loadNaraCommercialTurnContext } from '@/lib/nara-unit-queries';"
          if old not in webhook: raise SystemExit('Importação do webhook não encontrada')
          webhook = webhook.replace(old, new, 1)
          old = "  if (!history.length) {\n    await handleAiFailure({"
          # Insere depois do bloco de histórico vazio, usando um marcador estável logo antes de let turn.
          marker = "  let turn;\n  try {\n    turn = await generateAiTurn(lead, history, context);"
          replacement = "  if (lead.kind === 'cliente') {\n    context.commercial = await loadNaraCommercialTurnContext(\n      args.admin,\n      args.channel.organization_id,\n      lead,\n      history,\n    );\n  }\n\n  let turn;\n  try {\n    turn = await generateAiTurn(lead, history, context);"
          if marker not in webhook: raise SystemExit('Ponto de consulta no webhook não encontrado')
          webhook = webhook.replace(marker, replacement, 1)
          webhook_path.write_text(webhook, encoding='utf-8')

          route_path = Path('src/app/api/ai-training/route.ts')
          route = route_path.read_text(encoding='utf-8')
          old = "import { buildAiInstructions, generateAiTurn, type AiFileOption, type AiTrainingContext } from '@/lib/ai';\nimport {"
          new = "import { buildAiInstructions, generateAiTurn, type AiFileOption, type AiTrainingContext } from '@/lib/ai';\nimport { loadNaraCommercialTurnContext } from '@/lib/nara-unit-queries';\nimport {"
          if old not in route: raise SystemExit('Importação da rota de simulador não encontrada')
          route = route.replace(old, new, 1)
          old = "    const aiContext = makeAiContext(config, examples, files);\n    const turn = await generateAiTurn(lead, messages, aiContext);"
          new = "    const aiContext = makeAiContext(config, examples, files);\n    if (body.agent === 'nara') {\n      aiContext.commercial = await loadNaraCommercialTurnContext(\n        context.supabase,\n        context.organizationId,\n        lead,\n        messages,\n      );\n    }\n    const turn = await generateAiTurn(lead, messages, aiContext);"
          if old not in route: raise SystemExit('Ponto de consulta no simulador não encontrado')
          route = route.replace(old, new, 1)
          route_path.write_text(route, encoding='utf-8')

          package_path = Path('package.json')
          package = json.loads(package_path.read_text(encoding='utf-8'))
          package['scripts']['test:nara-unit-queries'] = 'node --experimental-strip-types scripts/check-nara-unit-queries.mjs'
          package_path.write_text(json.dumps(package, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')

          validate_path = Path('.github/workflows/validate.yml')
          validate = validate_path.read_text(encoding='utf-8')
          old = "      - name: Test Nara price levels\n        run: npm run test:nara-price-levels\n      - name: Validate environment"
          new = "      - name: Test Nara price levels\n        run: npm run test:nara-price-levels\n      - name: Test Nara unit queries\n        run: npm run test:nara-unit-queries\n      - name: Validate environment"
          if old not in validate: raise SystemExit('Ponto do CI permanente não encontrado')
          validate = validate.replace(old, new, 1)
          validate_path.write_text(validate, encoding='utf-8')
          PY

      - name: Instalar dependências
        run: npm ci --no-audit --no-fund

      - name: Testar contrato do prompt
        run: npm run test:nara-prompt

      - name: Testar camada fixa
        run: npm run test:nara-fixed-layer

      - name: Testar níveis de preço
        run: npm run test:nara-price-levels

      - name: Testar consultas de unidades
        run: npm run test:nara-unit-queries

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Gravar implementação validada
        shell: bash
        run: |
          git checkout -- next-env.d.ts tsconfig.json || true
          rm -f tsconfig.tsbuildinfo
          rm .github/workflows/phase5-nara-unit-queries.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/nara-unit-queries.ts src/lib/ai.ts src/lib/whatsapp/webhookProcessor.ts src/app/api/ai-training/route.ts scripts/check-nara-unit-queries.mjs package.json .github/workflows/validate.yml .github/workflows/phase5-nara-unit-queries.yml
          git commit -m "Integra consultas de unidades e preços à Nara"
          git push origin HEAD:feature/nara-prompt-v3-phase-5
